import styled from 'styled-components/native';
import { TouchableOpacity } from 'react-native';


export const ButtonItems = styled(TouchableOpacity)`
  margin-top: 5px;
  background-color: #a370f7;
  padding: 15px;
  border-radius: 14px;
  align-items: center;
`;

export const TextItems = styled.Text`
  color: #fff;
  font-weight: bold;

`;